# Google Maps API運用メモ

`transport_cost_simulator.html` の移動代金シミュレーターは、住所から距離と所要時間を取得し、既存の交通手段別単価へ反映する構成です。

## 現在の実装

- Maps JavaScript APIをページ内で読み込みます。
- Places Autocompleteで出発地・目的地の住所入力を補助します。
- `google.maps.DirectionsService` でルートを取得し、距離（km）と所要時間（分）へ変換します。
- 自動取得できない場合でも、距離と時間は手動入力で概算できます。
- 同一ページ内では、同じ出発地・目的地・交通手段の検索結果をキャッシュしてAPI呼び出しを抑えます。

## 有効化が必要なAPI

| API | 用途 |
| --- | --- |
| Maps JavaScript API | ブラウザでGoogle Maps機能を読み込む |
| Places API | 住所入力候補を表示する |
| Directions API | 出発地から目的地までの距離・所要時間を取得する |

ブラウザから `https://routes.googleapis.com/directions/v2:computeRoutes` を直接 `fetch` する方式は、CORSやAPIキー管理の面で静的サイトと相性が悪いため採用していません。

## APIキーの必須設定

1. **アプリケーション制限:** HTTPリファラー
2. **許可するリファラー:** `https://9z2fqjckjv-bot.github.io/*` など公開ドメインのみ
3. **API制限:** Maps JavaScript API、Places API、Directions APIのみ
4. **割り当て上限:** 1日あたりの上限を設定
5. **予算アラート:** 想定額に近づいたら通知されるように設定

APIキーを制限しないままHTMLへ埋め込むと、第三者利用や高額請求につながる可能性があります。

## 料金を抑える運用

- 入力中に自動検索せず、「距離・所要時間を自動取得」ボタン押下時だけAPIを呼び出す
- 地図表示は行わず、距離・時間・料金の数値表示に絞る
- 公開後はGoogle Cloud Consoleでリクエスト数、エラー率、課金見込み額を確認する
- 利用が増えた場合は、日次クォータや予算上限を見直す

## 公開前チェック

- 公開URLで住所候補が表示される
- 出発地・目的地を入力して距離と所要時間が反映される
- APIエラー時に利用者向けメッセージが表示され、手動入力に戻れる
- 交通手段を変えた場合にルート検索・料金計算が再実行できる
- スマートフォンで入力欄と計算結果が読みやすい
